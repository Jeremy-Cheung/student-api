import classes from "./TopBar.module.css";

export default function TopBar() {
    return (
        <div>
        <nav>
            <header className={classes.header}>
                STUDENT-API DEMO
            </header>
        </nav>
        </div>
    )
}